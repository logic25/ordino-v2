// Stub: project-side surfacing of relevant playbooks comes later.
// For now, callers receive null and render nothing.
export function useMarketForAddress(_address: string | null | undefined) {
  return { data: null as null, isLoading: false };
}
